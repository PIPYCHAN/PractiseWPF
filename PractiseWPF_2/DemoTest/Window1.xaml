<Window x:Class="PractiseWPF_2.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:PractiseWPF_2"
        Title="{x:Static local:Window1.WindowTitle}" Height="300" Width="300">
    <Window.Resources>
        <Style x:Key="{x:Type Button}" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <StackPanel>
        <TextBlock FontSize="32" Text="{x:Static local:Window1.ShowText}"/>
        <!--<ListBox>
            <ListBox.ItemsSource>
                <x:Array Type="sys:String">
                    <sys:String>Tim</sys:String>
                    <sys:String>Tom</sys:String>
                    <sys:String>Jack</sys:String>
                </x:Array>
                </ListBox.ItemsSource>
        </ListBox>-->
        
        <!--<Button Content="OK"></Button>
        <Button Content="OK"></Button>
        <Button Content="OK"></Button>
        <Button Content="OK" Style="{x:Null}"></Button>-->
    </StackPanel>
</Window>

    